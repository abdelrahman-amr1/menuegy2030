# تقرير التغييرات التقنية والمشروع (Developer Log & Integration Walkthrough)

يحتوي هذا الملف على التفاصيل التقنية الكاملة لكافة التعديلات، البنية البرمجية المحدثة، وتفاصيل ربط قاعدة البيانات السحابية بالمتجر وبمساعد كتالوج واتساب.

---

## 1. ربط قاعدة البيانات السحابية (Supabase Database Integration)
تم نقل البنية البرمجية للنظام من التخزين المحلي (LocalStorage) إلى قاعدة بيانات سحابية مركزية ومؤمنة باستخدام **Supabase**.

* **رابط المشروع السحابي:** `https://dvoodpzhkkwvhdzarags.supabase.co`
* **مفتاح الاتصال العام (Anon Key):** مستخدم في الواجهة الأمامية للمتجر ومساعد الكتالوج لاسترجاع البيانات بأمان.
* **الملف المسؤول:** [database.js](file:///c:/Users/pc/Downloads/api/menuegy2030/js/database.js)

### جداول قاعدة البيانات (Database Schema):
1. **جدول المتاجر (`shops`):** يحتوي على إعدادات وهوية كل متجر فرعي (المعرف `id`, الاسم، الألوان، أرقام الواتساب، وبيانات الأدمن).
2. **جدول المنتجات (`products`):** يحتوي على بضائع كل محل، ويرتبط بجدول المتاجر عبر العمود المشترك `shop_id`.

---

## 2. إدارة التصنيفات الديناميكية (Dynamic Categories System)
تم إلغاء تثبيت تصنيفات عطارة أسوان لتصبح التصنيفات مرنة وتعتمد بالكامل على متطلبات كل تاجر.

* **الملف المسؤول:** [admin.js](file:///c:/Users/pc/Downloads/api/menuegy2030/js/admin.js) عبر الدالتين `populateAdminCategorySelect` و `populateAdminCategoryFilter`.
* **آلية العمل:**
  * يتم فحص المنتجات الموجودة حالياً بالمتجر واستخراج التصنيفات الفريدة المستخدمة فعلياً وعرضها فقط.
  * في حالة المتاجر الجديدة (0 منتجات)، يتم توجيه التاجر تلقائياً إلى خيار **"إضافة تصنيف جديد..."** وفتح حقل الكتابة بشكل تلقائي لتبسيط التجربة.

---

## 3. آلية إعادة ضبط المتجر (Empty Reset Store Logic)
تغيير السلوك الافتراضي لزر إعادة تعيين المتجر ليعطي متجراً فارغاً ومستعداً للعمل فوراً.

* **الملفات المعدلة:** [admin.js](file:///c:/Users/pc/Downloads/api/menuegy2030/js/admin.js) و [database.js](file:///c:/Users/pc/Downloads/api/menuegy2030/js/database.js)
* **التفاصيل:**
  * تم تعريف دالة سحابية جديدة `clearShopProducts(shopSlug)` تقوم بمسح كافة منتجات المتجر من جدول المنتجات نهائياً.
  * تم استبدال رسالة التنبيه القديمة (التي كانت تخبر العميل بتهيئة بهارات أسوان) لتعلم التاجر بأنه سيتم تصفير المتجر ليصبح فارغاً بالكامل.

---

## 4. نظام رفع الصور السحابي (Supabase Storage Image Uploader)
تسهيل رفع صور المنتجات للأدمن مباشرة دون الحاجة لاستخدام روابط خارجية.

* **الملفات المعدلة:** [admin/index.html](file:///c:/Users/pc/Downloads/api/menuegy2030/admin/index.html) و [admin.js](file:///c:/Users/pc/Downloads/api/menuegy2030/js/admin.js) و [database.js](file:///c:/Users/pc/Downloads/api/menuegy2030/js/database.js)
* **البنية البرمجية:**
  * تم إنشاء مخزن للملفات السحابية (Storage Bucket) باسم `images` في Supabase.
  * تم تضمين زر رفع بجوار حقل رابط الصورة في نافذة المنتجات.
  * بمجرد اختيار ملف الصورة، تقوم دالة `uploadFileToSupabase` بتوليد اسم فريد ونظيف للصورة، رفعها سحابياً، وجلب رابطها المباشر (Public URL) وتعبئته تلقائياً في حقل الإدخال ليتم حفظه مع المنتج.

---

## 5. سلة مشتريات موبايل تفاعلية (Mobile Cart Bottom-Sheet)
تحسين واجهة الاستخدام على الهواتف الذكية لجعل تصفح السلة يبدو كتطبيق أصلي (Native UX).

* **الملف المعدل:** [style.css](file:///c:/Users/pc/Downloads/api/menuegy2030/css/style.css)
* **التصميم المطور:**
  * **على أجهزة الكمبيوتر:** تنزلق السلة كدرج جانبي أنيق من جهة اليمين (RTL Flow).
  * **على الموبايل (شاشات < 768px):** تتحول السلة تلقائياً إلى **Bottom Sheet (ستارة سفلية)** تنزلق بسلاسة من الأسفل إلى الأعلى عند الفتح وتختفي للأسفل عند الإغلاق، مع حواف دائرية علوية بمقدار `24px` وظل ناعم يعطي انطباعاً بالفخامة.

---

## 6. تكامل مساعد كتالوج واتساب التفاعلي (WhatsApp Catalog Helper Integration)
تمكين المبرمج/المدير من تعديل ونقل منتجات المتجر إلى كتالوج واتساب للأعمال في ثوانٍ معدودة.

* **الملف المعدل:** [catalog_helper.html](file:///C:/Users/pc/Downloads/api/eng.abdelrahman.amr/catalog_helper.html) (ملف مستقل خارج بيئة المستودع).
* **التفاصيل:**
  * تم دمج مكتبة Supabase JS SDK داخل ترويسة الملف.
  * تم تعديل كود الـ JavaScript ليقوم بإرسال طلب سحابي لجلب منتجات المتجر `am` المسجلة في السحابة ورسمها تفاعلياً.
  * تم برمجة نظام فلاتر وتصنيفات ديناميكية تتحدث تلقائياً حسب الأقسام المكتوبة بالمنتجات.
  * الحفاظ على مصفوفة المنتجات القديمة كـ **Fallback (نسخة احتياطية)** لتظهر مباشرة في حال كان المتصفح غير متصل بالإنترنت.
  * تنظيف الملف وتطهيره من بقايا الأكواد المتداخلة في النهاية ليعمل بدون أي أخطاء برمجة في المتصفح.

---

## 7. سكربت تغذية البيانات للمحفظة الخاصة بك (Database Seeder Script)
لتسهيل إطلاق محفظة خدماتك البرمجية على المتجر الجديد لتعمل كواجهة حية لعملائك.

* **الملف المنشأ:** `scratch/seed_supabase.js`
* **آلية التنفيذ:** تم تشغيل سكربت Node.js محلي باستخدام الـ `service_role` السري لقاعدة بياناتك، حيث قام بالخطوات التالية:
  1. إنشاء بروفايل متجر ديناميكي تحت اسم المعرف `am` وتثبيت إعداداته وألوانه الداكنة الفخمة.
  2. مسح أي عينات منتجات سابقة مسجلة للمتجر `am`.
  3. تغذية قاعدة البيانات وحقن **15 خدمة ونظام برمجيات (SaaS)** الخاصة بك دفعة واحدة بالأسعار، التفاصيل، الأوصاف وصور الخلفية الصحيحة.
